import { SpecializationCreateComponent } from './specialization-create.component'

describe('SpecializationCreateComponent', () => {
  it('should mount', () => {
    cy.mount(SpecializationCreateComponent)
  })
})