import { ListAllSpecializationComponent } from './list-all-specialization.component'

describe('ListAllSpecializationComponent', () => {
  it('should mount', () => {
    cy.mount(ListAllSpecializationComponent)
  })
})